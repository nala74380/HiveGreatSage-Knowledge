---
文件位置: 01-网络验证系统/测试基线.md
名称: 测试基线
作者: 蜂巢·大圣 (Hive-GreatSage)
时间: 2026-05-18
版本: V1.0.3
状态: 草稿
关联文档:
  - "[[00-项目总控/术语表]]"
  - "[[00-项目总控/文档代码测试追踪矩阵]]"
  - "[[01-网络验证系统/README]]"
  - "[[01-网络验证系统/实现状态对照表]]"
  - "[[01-网络验证系统/测试体系与质量门禁规范]]"
  - "[[01-网络验证系统/源码文件逐项审查清单]]"
变更记录:
  - V1.0.3 (2026-05-18): 批次12收口；补充用户治理单页化回归后结果 53 passed，并同步 OpenAPI schemas=96
  - V1.0.2 (2026-05-18): 批次11收口；补充 TZYMIR 最小回归 52 passed、frontend build 通过与 OpenAPI 快照导出结果
  - V1.0.1 (2026-05-12): 目录级治理第一批；补充测试基线证据边界，明确历史测试报告不等于当前测试状态
  - V1.0.0: Obsidian 去漂移重构生成
---

# 测试基线


## 当前结论

已确认：历史测试报告曾存在不一致，不能再直接沿用旧结论。

已确认：批次11已在 `TZYMIR` 环境跑通最小回归集并拿到新实测结果。

## 当前证据边界

- 本文记录历史测试基线与重新执行要求。
- 本文不代表全量 pytest 已通过。
- 本文仅代表本轮最小回归与构建结果已通过。
- 本文不代表 Celery / Redis / PostgreSQL 运行验证已通过。
- 本轮已执行 `conda run -n TZYMIR python -m pytest tests/test_admin.py tests/test_authorization_upgrade.py tests/test_accounting.py tests/test_device.py -q`，结果 `53 passed`。
- 本轮已执行 `frontend npm run build`，结果通过（含 chunk size warning）。
- 本轮已执行 `python scripts/export_openapi.py`，结果 `paths=112`、`schemas=96`。
- 新基线必须记录 Git commit、数据库环境、Redis 状态、pytest passed/failed/skipped、frontend build 和 Celery 覆盖情况。

## 历史报告状态

- 一份报告显示 95 个测试中 74 通过、21 失败。
- 另一份报告显示 95 个测试中 66 通过、29 错误。
- `test_result_raw.json` 曾显示 skipped 口径。
- 错误集中在 hive_user 访问 hive_game_001 的认证/权限问题、旧路径和旧请求体漂移。

## 本轮已执行

```bash
conda run -n TZYMIR python -m pytest tests/test_admin.py tests/test_authorization_upgrade.py tests/test_accounting.py tests/test_device.py -q
python scripts/export_openapi.py
cd frontend && npm run build
```

## 新基线必须记录

| 项 | 必填 |
|---|---|
| 测试时间 | 是 |
| 代码来源 | Git commit / zip 名称 |
| 数据库环境 | 主库 / 游戏库 |
| Redis 状态 | 是 |
| pytest passed | 是 |
| pytest failed | 是 |
| pytest skipped | 是，必须说明原因 |
| frontend build | 是 |
| Celery 相关测试 | 若未覆盖必须说明 |

## 当前仍不能声称

- 不能声称全量测试全绿。
- 不能把“最小回归通过”替代“全量回归通过”。
- 不能把后端 pytest 当成三端联调通过。
