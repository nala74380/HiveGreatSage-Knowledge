---
文件位置: 01-网络验证系统/风险整改记录.md
名称: 风险整改记录
作者: 蜂巢·大圣 (Hive-GreatSage)
时间: 2026-05-06
版本: V1.0.0
状态: 草稿
关联文档:
  - "[[01-网络验证系统/实现状态对照表]]"
  - "[[01-网络验证系统/旧字段旧接口清理清单]]"
  - "[[01-网络验证系统/API鉴权方案]]"
  - "[[01-网络验证系统/API路由清单]]"
变更记录:
  - V1.0.0 (2026-05-06): 建立 Verify 风险整改提交记录，区分已提交、待提交、待验证
---

# 风险整改记录

← 返回 [[项目总大纲]] | 父节点: [[01-网络验证系统/架构设计]]

## 一、记录目的

本文用于记录 Verify 风险整改的真实提交状态。

原则：

```text
已提交：GitHub main 上已经能看到对应源码或文档变化。
已确认：通过 GitHub 文件复核或接口返回确认。
待提交：已经确定整改方向，但尚未提交到 GitHub。
待验证：源码已改，但尚未运行 pytest、前端 build、迁移或联调。
```

本文不把“计划整改”写成“已经完成”。

## 二、已确认提交项

| 编号 | 仓库 | 状态 | 说明 |
|---|---|---|---|
| FIX-VERIFY-001 | HiveGreatSage-Verify | 已确认 | `app/routers/balance_admin.py` 已不存在，旧 balance admin 空路由已清理。 |
| FIX-VERIFY-002 | HiveGreatSage-Verify | 已确认 | `app/routers/balance_agent.py` 已不存在，旧 balance agent 空路由已清理。 |
| FIX-VERIFY-003 | HiveGreatSage-Verify | 已确认 | `scripts/setup_game_db.py` 已不存在，危险误命名 reset 副本已清理。 |
| FIX-VERIFY-004 | HiveGreatSage-Verify | 已确认 | `GET /api/users/{user_id}/authorizations/{auth_id}/upgrade/preview` 已接入 `_resolve_caller` 与 `preview_authorization_upgrade()`。 |
| FIX-VERIFY-005 | HiveGreatSage-Verify | 已提交 / 待运行验证 | 心跳限流已从单 `user_id` 改为 `user_id + game_project_code + device_fingerprint`。提交：`3024ac3629c52f78555a3b11543595987bb81323`。 |
| FIX-KNOW-001 | HiveGreatSage-Knowledge | 已确认 | `API鉴权方案.md` 已改为当前源码口径：User AT 使用 `authorization_level/project_code`，Refresh Token 为不透明随机字符串。 |
| FIX-KNOW-002 | HiveGreatSage-Knowledge | 已确认 | `API路由清单.md` 已修正用户管理路径为 `/api/users/*`，不再写 `/admin/api/users/*`。 |
| FIX-KNOW-003 | HiveGreatSage-Knowledge | 已确认 | `旧字段旧接口清理清单.md` 已明确开发期不兼容旧字段、旧接口。 |

## 三、待提交项

| 编号 | 模块 | 优先级 | 说明 |
|---|---|---|---|
| TODO-FE-001 | `frontend/src/views/shared/UserDetail.vue` | P1 | 授权新增弹窗重置表单仍需确认是否保留 `user_level`、`authorized_devices` 默认值。 |
| TODO-FE-002 | `frontend/src/views/shared/UserDetail.vue` | P1 | 授权升级页面仍需改为 `upgradePreview + upgradeAuth` 专用接口，而不是通过 `updateAuth` 增加设备数。 |
| TODO-FE-003 | `frontend/src/views/shared/UserDetail.vue` | P1 | Agent 视角设备绑定卡片应隐藏，或新增 Agent 可访问的用户设备接口。 |
| TODO-AGENT-001 | `/api/agents/me` | P1 | 需要返回完整业务能力快照并绑定 `response_model`，供菜单、TopBar、AgentProfile 共用。 |
| TODO-ACCOUNTING-001 | `accounting_service.py` | P1 | 钱包扣点、充值、授信、冻结等写操作需要显式行锁或原子更新；只读接口不应加锁。 |
| TODO-DEVICE-001 | `device_admin.py` | P1 | 后台全局设备列表需要避免全量 SQL + 全量 Redis SCAN + Python 分页。 |
| TODO-UPDATE-001 | `update_admin.py` | P1 | 热更新上传应从一次性读 500MB 文件改为流式读取、哈希和临时文件原子落盘。 |

## 四、待验证项

| 编号 | 验证项 | 状态 |
|---|---|---|
| VERIFY-TEST-001 | `python -m compileall app tests scripts` | 待重新运行 |
| VERIFY-TEST-002 | `tests/test_authorization_upgrade.py` | 待运行 |
| VERIFY-TEST-003 | 心跳限流多设备场景测试 | 待补测试 |
| VERIFY-TEST-004 | 前端 `npm ci && npm run build` | 待运行 |
| VERIFY-TEST-005 | OpenAPI 快照导出 | 待脚本落地 |
| VERIFY-TEST-006 | 账务并发扣点测试 | 待补测试 |

## 五、禁止回退项

```text
禁止恢复 User.user_level / User.max_devices / User.expired_at。
禁止恢复 Token 字段 level / project。
禁止恢复 balance_admin.py / balance_agent.py。
禁止恢复 scripts/setup_game_db.py 作为 reset 脚本。
禁止恢复 /admin/api/users/* 作为用户管理主路径。
禁止恢复 Admin 硬删除用户、代理、项目的主线接口。
```

## 六、下一步整改顺序

建议顺序：

```text
1. 修 UserDetail 授权新增与升级专用接口。
2. 补 /api/agents/me 完整业务能力快照。
3. 补 accounting wallet 行锁或原子更新。
4. 拆 device_admin 全局设备列表性能风险。
5. 热更新上传流式化。
6. 补对应测试与前端 build 验证。
```
