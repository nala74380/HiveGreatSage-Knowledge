---
文件位置: 01-网络验证系统/风险整改记录.md
名称: 风险整改记录
作者: 蜂巢·大圣 (Hive-GreatSage)
时间: 2026-05-10
版本: V2.0.0
状态: 定稿
关联文档:
  - "[[01-网络验证系统/实现状态对照表]]"
  - "[[01-网络验证系统/旧字段旧接口清理清单]]"
  - "[[01-网络验证系统/架构设计]]"
变更记录:
  - V2.1.0 (2026-05-10): 精读整改同步——修复参数测试清理 SQL、Android 参数保存响应判断、Android OCR 授权码硬编码、热更新/账务文档漂移
  - V2.0.0 (2026-05-10): 全量审查后同步——13 项修复记录 + 旧项状态更新 + 受阻项全部解除
  - V1.0.2 (2026-05-06): SideMenu 改为 can_create_sub_agents
  - V1.0.1 (2026-05-06): UserDetail 整改
  - V1.0.0 (2026-05-06): 初始建立
---

# 风险整改记录

← 返回 [[项目总大纲]] | 父节点: [[01-网络验证系统/架构设计]]

## 2026-05-09~10 审查修复（13 项 — 全部已提交）

| 编号 | 等级 | 模块 | 问题 | 状态 |
|------|------|------|------|------|
| FIX-001 | P0 | `heartbeat_flush.py` | _HEALTH_KEY 语法错误 | ✅ |
| FIX-002 | P0 | `crypto.py` | Fernet key 未 base64 编码 | ✅ |
| FIX-003 | P0 | `dependencies.py` | get_game_project_code 不校验 is_active | ✅ |
| FIX-004 | P0 | `models.py` + 迁移 0021 | VersionRecord 缺 partial unique index | ✅ |
| FIX-005 | P0 | `storage/s3.py, factory.py` | S3 占位→完整 boto3 实现 | ✅ |
| FIX-006 | P1 | `auth_service.py` | revoke_all docstring 过时 | ✅ |
| FIX-007 | P1 | `device_service.py` | 离线阈值 90→120s | ✅ |
| FIX-008 | P1 | `accounting_service.py` | 幂等键冲突阻塞重复扣点 | ✅ |
| FIX-009 | P1 | `agent_scope_management.py` | _transfer_points 缺 for_update | ✅ |
| FIX-010 | P1 | `users.py` | _resolve_caller 跳过黑名单 | ✅ |
| FIX-011 | P1 | `device_admin.py` | _get_admin_or_agent 跳过黑名单 + 离线阈值 | ✅ |
| FIX-012 | P1 | `agents.py` | 代理创建缺 can_create_sub_agents 检查 + delete 环境门控 | ✅ |
| FIX-013 | P2 | `UploadVersionForm.vue` | axios 裸调 + 缺文件校验 | ✅ |

## 旧项状态更新

| 编号 | 原状态 | 新状态 |
|------|--------|--------|
| BLOCK-AGENT-001 | AgentMeResponse 受阻 | ✅ 已落地 (schemas/agent_me.py 存在) |
| BLOCK-AGENT-002 | agent_profile_service hierarchy_level | ✅ 已修复 |
| TODO-ACCOUNTING-001 | 钱包行锁 | ✅ 已修复 (for_update 全链路) |
| TODO-DEVICE-001 | 全局设备列表全量 SCAN | ✅ 已优化 (在线集合 + 项目级查询) |
| TODO-UPDATE-001 | 热更新流式上传 | ✅ 已实现 (分块复制 + 原子替换 + SHA-256) |
| FIX-VERIFY-005 | 心跳限流粒度 | ✅ 已确认 (per user+project+device) |
| FIX-VERIFY-007 | hierarchy_depth 响应匹配 | ✅ 已确认 |

## 2026-05-10 精读整改追加

| 编号 | 等级 | 模块 | 问题 | 处理 |
|------|------|------|------|------|
| FIX-014 | P1 | `tests/test_params.py` | teardown 使用 `user_script_param.param_key`，但该表无此字段 | ✅ 改为通过 `param_def_id IN (SELECT id FROM script_param_def WHERE param_key LIKE 'test_%')` 清理 |
| FIX-015 | P1 | `AndroidScript/framework/cloud_sync.lua` | `save_params()` 按旧 `message` 字段判断成功，和 Verify 当前 `ParamsSetResponse` 不一致 | ✅ 改为检查 `updated_count` / `failed_count` |
| FIX-016 | P1 | `AndroidScript/config.lua` | 源码中硬编码 OCR 授权码样式内容 | ✅ 清空为运行时/私有配置注入占位 |
| FIX-017 | P1 | `requirements.txt` | 未固定 Starlette 兼容范围，当前环境 `starlette==1.0.0` 导致 FastAPI 0.115.6 加载失败 | ✅ 增加 `starlette>=0.40.0,<0.42.0` 并在本地安装 0.41.3 后验证 |
| FIX-018 | P1 | `tests/test_accounting.py` | 硬编码 `PROJECT_ID = 1`，且扣点场景误用 Admin Token 断言代理扣点 | ✅ 改为 `project_id` fixture，并使用 Agent Token 创建用户/授权/升级；`pytest tests/test_accounting.py` 12 passed |
| FIX-019 | P1 | `app/routers/auth.py` + `tests/test_auth.py` | revoke-all 路由注释仍写“其它 AT 自然过期前可能有效”，与 token_version 实现不符 | ✅ 注释改为 token_version 强校验；现有 `test_revoke_all_invalidates_all_access_tokens` 覆盖旧 AT/RT 失效 |
| DOC-001 | P1 | `热更新端到端测试清单.md` | 仍把“客户端读游戏库 version_record”描述为当前 P0 阻塞 | ✅ 改为源码层已关闭，保留端到端实测要求 |
| DOC-002 | P1 | `代理点数计费与返点规则.md` | 旧点数体系文档仍大量称旧对象为“当前源码” | ✅ 标记为历史口径，当前以 `账务中心规则.md` 与 accounting center 源码为准 |

## 仍未关闭项

| 编号 | 等级 | 模块 | 问题 | 下一步 |
|------|------|------|------|------|
| TODO-TEST-001 | P0 | 项目准入 | 缺独立 `tests/test_project_access.py` | 补 public / level_limited / invite_only / hidden、manual/auto/disabled、越权与用户授权边界测试 |
| TODO-TEST-002 | P0 | 设备绑定项目维度 | 缺独立项目维度测试 | 补 PC 不占设备数、Android 占设备数、同设备跨项目隔离、A 项目绑定不放行 B 项目心跳 |
| TODO-UPDATE-002 | P1 | AndroidScript 热更新 | 收到 `checksum_sha256` 但尚未做文件 SHA-256 校验 | 确认懒人精灵可用 SHA-256 API 或接入插件/服务端签名校验方案 |
| TODO-ACCOUNTING-003 | P1 | 账务并发/回滚 | 并发扣点、事务失败回滚仍未充分测试 | 补并发授权与故障注入测试 |
| TODO-FRONTEND-001 | P2 | 热更新前端 API | 上传组件已走统一 `http`，但仍未抽 `adminUpdateApi` | 需要时新增 `frontend/src/api/admin/update.js` 管理 upload/latest/history |

## 禁止回退项（维护中）

```
禁止恢复 User.user_level / User.max_devices / User.expired_at。
禁止恢复 Token 字段 level / project。
禁止恢复 balance_admin.py / balance_agent.py。
禁止恢复 hierarchy_level 作为对外代理组织层级字段。
禁止恢复 code_name 作为对外项目编码字段。
禁止 IMSI 明文存储。
禁止独立路由器跳过 Redis 黑名单校验。
```

## 下一步

```text
1. 运行 pytest test_accounting.py — 验证财务链路
2. 三端联调 (PCControl + AndroidScript + Verify)
3. 1000+ 设备心跳压测
4. 生产环境部署 (Nginx + HTTPS + systemd)
```
