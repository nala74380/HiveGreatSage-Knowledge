---
文件位置: 01-网络验证系统/实现状态对照表.md
名称: 实现状态对照表
作者: 蜂巢·大圣 (HiveGreatSage)
时间: 2026-05-16
版本: V1.2.0
状态: 草稿
关联文档:
  - "[[00-项目总控/术语表]]"
  - "[[00-项目总控/文档代码测试追踪矩阵]]"
  - "[[01-网络验证系统/README]]"
  - "[[01-网络验证系统/架构设计]]"
  - "[[01-网络验证系统/测试基线]]"
  - "[[01-网络验证系统/三端联调测试清单]]"
  - "[[01-网络验证系统/风险与待决策清单]]"
  - "[[01-网络验证系统/账务中心规则]]"
  - "[[01-网络验证系统/热更新端到端测试清单]]"
  - "[[02-PC中控框架/Verify接口调用清单]]"
  - "[[03-安卓脚本框架/热更新方案]]"
变更记录:
  - V1.2.0 (2026-05-16): 同步 TZYMIR 实跑证据（auth/device/device_admin/params/update/update_admin 共 55 passed, 10 skipped）；同步 frontend build 成功；更新测试体系状态口径
  - V1.1.2 (2026-05-12): 目录级治理第一批；补充实现状态证据边界，明确"✅ 已修复 / ✅ 已实现"表示源码层或历史治理登记，不等于当前 R1 / E2 / M1 闭环
  - V1.1.1 (2026-05-03): 修正热更新状态表达；从"已修复"调整为"源码层已修复 / 待运行验证 / 待三端联调"，避免把未实测内容写成已通过
  - V1.1.0 (2026-05-03): 热更新真相源已统一；旧 ORM 模型已删除；旧路由已迁移；Redis KEYS 风险已修复；auth_service 日期比较已修复
  - V1.0.0: Obsidian 去漂移重构生成
---

# 实现状态对照表


## 当前证据边界

- 本表是 Verify 当前实现状态的阶段性登记表。
- "✅ 已修复 / ✅ 已实现"只表示源码层或历史治理批次登记状态。
- 本轮已在 `conda activate TZYMIR` 环境重新运行核心 pytest 子集（auth/device/device_admin/params/update/update_admin）。
- 本轮未重新运行 Alembic。
- 本轮已重新运行 frontend build（`npm run build` 通过）。
- 本轮未重新完成三端联调。
- 不得把本表中的"已修复 / 已实现"直接写成"生产可用 / 已闭环 / 可发布"。

## 状态分级

| 状态 | 含义 |
|---|---|
| 源码已存在 | 静态源码中已看到模型、路由、服务或页面实现 |
| 源码层已修复 | 已修改源码或测试，但尚未通过运行验证 |
| 待运行验证 | 需要实际启动、迁移、测试或构建确认 |
| 待三端联调 | 需要 Verify + PCControl + AndroidScript 联调 |
| 待生产化 | 需要部署、安全、压测、监控、回滚闭环 |
| 待修订 | 文档或代码口径已发现漂移 |
| 已登记完成 | 历史治理批次中被登记为完成，仍需按证据等级复核 |

## Verify 当前状态

> 最后更新: 2026-05-16（基于当前生产代码收口同步）

| 模块 | 当前状态 | 说明 |
|---|---|---|
| 认证登录 | 源码已存在 / 待联调 | 登录使用 Authorization 项目级口径；refresh 已改为 Authorization.user_level |
| revoke-all | ✅ 已修复（token_version 已实现） | user.token_version 递增 → 所有旧 AT 即时 401；RT 同步删除 |
| 用户授权 | ✅ 已修复 | 新口径 Authorization.user_level / authorized_devices / valid_until 已全线使用 |
| 设备绑定 | ✅ 已重构并补测试 | `DeviceBinding` 当前以 `user_id + game_project_id + device_fingerprint` 唯一；新增 `device_id`（用户自定义设备编号）与 `connection_type` / `connection_label`；`tests/test_device.py` 已补 PC 不占设备数、Android 占设备数、A 项目绑定并在线不放行 B 项目心跳 |
| Redis 心跳 | 源码已存在 / 已优化 / 待压测 | 在线设备集合替代 SCAN；心跳 Value 已补 `device_id` / `connection_type` / `connection_label`；/health/workers 端点监控 Celery |
| 设备标识体系 | 源码层已收口 / 待三端实机验证 | `device_fingerprint` = 内部稳定绑定键；`device_id` = 用户自定义设备编号；`connection_type` / `connection_label` = 连接标识；AndroidScript 与 PCControl 代码已开始对齐，仍待实机验证 USB / TCP 采集值 |
| IMSI 上传 | ✅ 已从主链删除 | `device_binding` 已移除 IMSI 列；`/api/device/imsi` 已删除；AndroidScript 不再依赖 IMSI 回退 |
| 热更新 | 源码层已进一步收口 / 待三端联调 | VersionRecord 主库唯一真相源；partial unique index 保证单 active；管理端发布事务已下沉到 `update_service.py`，`UpdateManage.vue` / `UploadVersionForm.vue` 继续沿用 `project_id + client_type` 管理端链路；AndroidScript 完整性校验与实机安装闭环仍待补证据 |
| 存储抽象 | ✅ 已实现（S3 完成） | LocalStorage 可用；S3Storage 完整实现（boto3，兼容 MinIO/OSS） |
| 账务中心 | 已清理 / 待回归验证 | 旧表已删除；新 accounting_* 模型完整 |
| 项目准入 | 源码已存在 / 账务口径待修 | 准入使用 tier_level，自动开通余额已读 AccountingWallet |
| 系统设置 / D 模式 | 源码已存在 / 三端待联调 | Web 设置页已支持，启动时自动初始化默认配置 |
| 统计 / Dashboard | 源码层已进一步收口 / 待运行验证 | `/admin/api/dashboard` 已收口为工作台聚合链；`/api/stats/platform` 保持标准摘要链，`total_devices_online` 已由 stats_service.py 统一产出，不再由 stats.py 路由层补丁注入；两条链仍为双轨职责分工 |
| 后台设备监控 | 源码层已收口 / 浏览器待验收 | `DeviceList` 已按 `listAll / summary / search` 分流；顶部卡片文案改为“当前筛选”；后台链当前直接展示 `device_id`、`device_fingerprint` 与连接标识 |
| 管理后台 | 源码已存在 / 浏览器待验收 | Vue 页面已覆盖主要功能；Dashboard 与 DeviceList 新口径已同步到当前生产代码 |
| 测试体系 | 核心子集 R1 已补齐 / 待扩展 | 已在 `TZYMIR` 环境运行 `test_auth.py`、`test_device.py`、`test_device_admin.py`、`test_params.py`、`test_update.py`、`test_update_admin.py`，结果 `55 passed, 10 skipped`；其余用例与三端联调仍待继续补证据 |
| 部署 | systemd 已完成 / 生产化待补 | API/Worker/Beat 三服务 systemd 文件已就绪；Nginx 配置待补 |

## 推荐下一步

当前最高优先级不是继续扩写新功能，而是先关闭验证口：

```text
1. 按 01-网络验证系统/开发启动流程.md 启动开发环境：WSL PostgreSQL / Redis + PowerShell conda activate TZYMIR。
2. 重新运行 tests/test_admin.py 与 tests/test_device_admin.py，确认测试契约回收后的最小验证集。
3. 重新运行 tests/test_update.py 与 tests/test_update_admin.py。
4. 浏览器验收 Dashboard 与 DeviceList：确认 active_projects_data、summary/search 分流与顶部统计口径。
5. 继续 AndroidScript 热更新 SHA-256 校验与实机安装闭环。
```

剩余重点工作：pytest 环境校正、浏览器验收、Android 热更新实机闭环、部署生产化补齐。
