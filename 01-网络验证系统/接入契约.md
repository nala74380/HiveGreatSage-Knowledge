---
文件位置: 01-网络验证系统/接入契约.md
名称: 接入契约
作者: 蜂巢·大圣 (HiveGreatSage)
时间: 2026-05-12
版本: V1.0.2
状态: 草稿
关联文档:
  - "[[00-项目总控/术语表]]"
  - "[[00-项目总控/文档代码测试追踪矩阵]]"
  - "[[01-网络验证系统/README]]"
  - "[[01-网络验证系统/API路由清单]]"
  - "[[01-网络验证系统/OpenAPI快照与接口契约治理规范]]"
  - "[[02-PC中控框架/Verify接口调用清单]]"
  - "[[03-安卓脚本框架/Verify接口调用清单]]"
变更记录:
  - V1.0.2 (2026-05-12): 目录级治理第一批；补充接入契约证据边界，明确 PCControl / AndroidScript 字段契约需以 OpenAPI 快照、源码和联调结果复核
  - V1.0.1: 同步 Verify 热更新链路小修复；明确客户端 check/download 已改为读取主库 VersionRecord，仍待运行测试验证
  - V1.0.0: Obsidian 去漂移重构生成
---

# 接入契约


## 当前定位

本文件描述 PCControl 与 AndroidScript 对接 Verify 的稳定契约。字段细节以 OpenAPI 快照为准。

## 当前证据边界

- 本文是 PCControl 与 AndroidScript 对接 Verify 的契约说明。
- 本文不替代 OpenAPI 快照。
- 本文不代表 PCControl / AndroidScript 当前已经完成所有字段消费。
- 本轮未重新运行三端联调。
- 涉及发布、联调、生产可用判断时，必须同时复核 Verify main、PCControl main、AndroidScript main、OpenAPI 快照和实际调用结果。

## 项目标识边界

| 标识 | 使用场景 |
|---|---|
| project_uuid | PC / Android 终端登录 Verify |
| project_id | 管理后台内部主键，热更新上传等后台接口 |
| project_code / game_project_code | 业务展示、查询、兼容接口 |

## 授权口径

- User 是账号主体。
- Authorization 是项目级授权主体。
- 用户等级、授权设备数、到期时间必须来自 Authorization。
- 响应字段可兼容 `user_level`，但来源不得再是 User.user_level。

## 设备口径

- Android 设备占用 Authorization.authorized_devices。
- PC 中控登录不占用 Android 授权名额。
- DeviceBinding 以 `user + game_project + device_fingerprint` 建模。
- `device_fingerprint` = 设备内部稳定绑定键，用于绑定、心跳归属和运行时数据关联。
- `device_id` = 安卓端用户自定义设备编号，用于业务展示和人工识别，不参与绑定唯一性判断。
- `connection_type` / `connection_label` = 连接标识：USB 显示 `SN:xxxx`，TCP 显示 `IP:端口`。
- 终端用户设备链 `/api/device/*` 当前以 `device_fingerprint` 作为单设备查询参数；`device_id`、连接标识作为展示字段返回。
- 后台设备监控链 `/admin/api/devices/*` 与用户详情设备绑定链 `/admin/api/users/{user_id}/devices` 当前直接返回设备原文字段，不再使用脱敏或摘要展示。

## 设备标识边界

- `/api/device/imsi` 已从主链删除。
- AndroidScript 不再依赖 IMSI 作为设备稳定绑定键回退来源。
- 当前设备标识体系以 `device_fingerprint + device_id + connection_type + connection_label` 为准。

## 热更新

- 客户端：`/api/update/check`、`/api/update/download`。
- 管理端：`/admin/api/updates/{project_id}/{client_type}`。
- VersionRecord 主库化：源码层已统一（D014）；待三端联调验证客户端 check/download。
- 强制更新失败时，AndroidScript / PCControl 不应继续运行旧版本。

## 网络配置

- `/api/client/network-config` 用于 D 模式网络配置下发。
- PCControl / AndroidScript 是否已消费该接口：待源码复核与三端联调。
