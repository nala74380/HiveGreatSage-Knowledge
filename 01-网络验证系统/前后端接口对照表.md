---
文件位置: 01-网络验证系统/前后端接口对照表.md
名称: Verify 前后端接口对照表
作者: 蜂巢·大圣 (Hive-GreatSage)
时间: 2026-05-18
版本: V1.1.2
状态: 草稿
关联文档:
  - "[[00-项目总控/项目总大纲]]"
  - "[[01-网络验证系统/API路由清单]]"
  - "[[01-网络验证系统/实现状态对照表]]"
  - "[[00-项目总控/文档代码测试追踪矩阵]]"
  - "[[01-网络验证系统/接入契约]]"
  - "[[01-网络验证系统/OpenAPI快照与接口契约治理规范]]"
变更记录:
  - V1.1.2 (2026-05-18): 用户授权升级模式扩展为 append / average / topup_align；前端 UserList 编辑抽屉与后端升级接口口径对齐
  - V1.1.1 (2026-05-17): 用户管理列表筛选口径对齐源码；下线 creator_agent_tier_level / creator_agent_can_create_sub_agents / creator_agent_risk_status（开发期不兼容）
  - V1.1.0 (2026-05-16): 基于 main.py + frontend/src/api + 关键页面调用 + TZYMIR 测试结果重写；移除旧 balance_* 路由与旧门面口径；补充 device_id/hash 与 HMAC-SHA256 口径
  - V1.0.0 (2026-04-29): 初版对照表
---

# Verify 前后端接口对照表

← 返回 [[00-项目总控/项目总大纲]] | 父节点: [[01-网络验证系统/API路由清单]]

## 一、文档边界

本文只回答三件事：

```text
1. 后端当前实际挂载了哪些路由前缀。
2. 前端当前从哪些 API 文件调用这些路由。
3. 哪些口径已经对齐，哪些仍待联调或待实机。
```

本文不是 OpenAPI 真相源。
字段、响应体、状态码以 FastAPI `/docs` 与源码为准。

## 二、本轮复核证据（2026-05-16）

```text
源码复核:
- HiveGreatSage-Verify/app/main.py
- HiveGreatSage-Verify/app/routers/*
- HiveGreatSage-Verify/frontend/src/api/*
- HiveGreatSage-Verify/frontend/src/views/*（关键调用点）

测试证据（TZYMIR）:
- python -m pytest -q tests/test_auth.py tests/test_device.py tests/test_device_admin.py
  tests/test_params.py tests/test_update.py tests/test_update_admin.py
- 结果: 55 passed, 10 skipped

构建证据:
- frontend npm run build
- 结果: build 成功（存在 chunk size warning）
```

## 三、后端路由挂载总览（main.py）

### 3.1 终端 User 链

```text
/api/auth/*
/api/device/*
/api/params/*
/api/update/*
/api/stats/*
/api/client/*
```

### 3.2 代理链

```text
/api/agents/auth/login
/api/agents/session/logout
/api/agents/me
/api/agents/me/dashboard
/api/agents/my/balance
/api/agents/my/transactions
/api/agents/my-projects
/api/agents/my/project-access/*
/api/agents/scope/*
/api/agents/*
```

### 3.3 用户管理共用链（Admin/Agent）

```text
/api/users/*
```

### 3.4 管理后台链

```text
/admin/api/auth/login
/admin/api/session/logout
/admin/api/dashboard
/admin/api/login-logs/
/admin/api/projects/*
/admin/api/prices/*
/admin/api/updates/*
/admin/api/devices/*
/admin/api/system-settings/*
/admin/api/accounting/*
/admin/api/project-access/*
/admin/api/agent-level-policies*
/admin/api/agents/{agent_id}/business-profile
/admin/api/agents/{agent_id}/password
/admin/api/audit-logs/*
/admin/api/users/{user_id}/devices*
```

## 四、前端 API 文件分层（当前真实结构）

### 4.1 管理后台 API

```text
src/api/admin/project.js
src/api/admin/device.js
src/api/admin/projectAccess.js
src/api/admin/balance.js
src/api/admin/agentProfile.js
src/api/admin/user.js
```

### 4.2 代理 API

```text
src/api/agent/balance.js
src/api/agent/projectAccess.js
```

### 4.3 共用/聚合 API

```text
src/api/auth.js
src/api/user.js
src/api/agent.js
src/api/agentScope.js
src/api/accounting.js
src/api/systemSettings.js
src/api/audit.js
src/api/client/device.js
```

说明：当前仓库无 `src/api/shared/*` 目录；旧文档中 shared 门面口径已过期。

## 五、关键接口对照（精简版）

| 能力 | 前端调用 | 后端路由 | 当前结论 |
|---|---|---|---|
| 管理员登录 | `src/api/auth.js` | `POST /admin/api/auth/login` | 已对齐 |
| 代理登录 | `src/api/auth.js` | `POST /api/agents/auth/login` | 已对齐 |
| 用户登录 | 终端链路（PC/Android） | `POST /api/auth/login` | 已对齐 |
| 用户管理 | `src/api/user.js` | `/api/users/*` | 已对齐 |
| 项目管理 | `src/api/admin/project.js` | `/admin/api/projects/*` | 已对齐 |
| 代理项目授权 | `src/api/admin/project.js` | `/admin/api/agents/{agent_id}/project-auths/*` | 已对齐 |
| 项目准入（管理端） | `src/api/admin/projectAccess.js` | `/admin/api/project-access/*` | 已对齐 |
| 项目准入（代理端） | `src/api/agent/projectAccess.js` | `/api/agents/my/project-access/*` | 已对齐 |
| 设备监控（后台） | `src/api/admin/device.js` | `/admin/api/devices/*` | 已对齐 |
| 设备数据（终端） | `src/api/client/device.js` | `/api/device/list` `/api/device/data` | 已对齐 |
| 热更新上传 | `UploadVersionForm.vue` 等 | `POST /admin/api/updates/{project_id}/{client_type}` | 已对齐 |
| 热更新客户端检查/下载 | 终端链路 | `GET /api/update/check` `/api/update/download` | 已对齐 |
| 账务中心 | `src/api/accounting.js` | `/admin/api/accounting/*` | 已对齐 |

### 5.1 用户管理列表筛选口径（2026-05-17）

`/api/users/` 当前前后端一致口径：

```text
前端传参与后端支持（保留）:
- page
- page_size
- status
- level
- project_id
- creator_agent_id

已下线（开发期不兼容）:
- creator_agent_tier_level
- creator_agent_can_create_sub_agents
- creator_agent_risk_status
```

### 5.2 用户授权升级模式口径（2026-05-18）

`/api/users/{user_id}/authorizations/{auth_id}/upgrade/preview` 与 `/upgrade` 当前统一 mode：

```text
- append
- average
- topup_align
```

当前前端入口：`frontend/src/views/shared/UserList.vue` 授权升级弹窗（新增设备）中的 `计费模式` 单选组已对齐上述三模式。

## 六、已确认的旧口径漂移

### 6.1 已删除/已过期文件口径

```text
app/routers/balance_admin.py  （不存在）
app/routers/balance_agent.py  （不存在）
```

旧文档若继续引用上述文件，按“过期文档”处理。

### 6.2 用户删除路径口径

当前主链是：

```text
DELETE /api/users/{user_id}
```

`/admin/api/users/{user_id}` 在当前代码中不是用户删除主链，不应作为通用删除路径引用。

### 6.3 设备监控字段口径

`/admin/api/devices/{game_project_code}` 当前返回的是：

```text
device_id_masked
device_id_hash
```

不是 `device_fingerprint_*` 字段。

### 6.4 敏感 hash 算法口径

当前敏感哈希由 `hash_sensitive_value()` 生成，算法为：

```text
HMAC-SHA256(SECRET_KEY, value)
```

不是纯 `sha256(value)`。

## 七、当前风险与待联调

```text
1. 已有 R1 仅覆盖 Verify 单仓核心链路；未到 E2/M1。
2. AndroidScript 热更新文件级 SHA-256 校验仍未闭环。
3. frontend build 成功，但 chunk 体积警告仍需后续优化。
4. /admin/api/devices/* 与 /api/device/* 双链路已分层，仍需三端联调验证调用边界。
```

## 八、下一步建议

```text
1. 继续 E2：Verify + PCControl + AndroidScript 联调（登录、心跳、参数、热更新）。
2. 继续 M1：安卓实机验证 installLrPkg + 文件级 hash 校验闭环。
3. 导出一版 OpenAPI 快照，和本表做自动化 diff。
4. 在文档治理清单中标记旧版《前后端接口对照表》条目已收敛。
```
