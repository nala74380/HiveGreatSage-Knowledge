---
文件位置: 01-网络验证系统/账务中心规则.md
名称: 账务中心规则
作者: 蜂巢·大圣 (Hive-GreatSage)
时间: 2026-05-12
版本: V1.0.1
状态: 草稿
关联文档:
  - "[[00-项目总控/术语表]]"
  - "[[00-项目总控/文档代码测试追踪矩阵]]"
  - "[[01-网络验证系统/README]]"
  - "[[01-网络验证系统/数据库模型清单]]"
  - "[[01-网络验证系统/数据库迁移对照表]]"
  - "[[01-网络验证系统/旧字段旧接口清理清单]]"
  - "[[01-网络验证系统/代理点数计费与返点规则]]"
  - "[[01-网络验证系统/风险与待决策清单]]"
变更记录:
  - V1.0.1 (2026-05-12): 第二批 B 账务专题治理；同步当前账务中心 10 模型口径，补入 AuthorizationFreezeRecord，修正旧表已删除过强表述并补充证据边界
  - V1.0.0: Obsidian 去漂移重构生成
---

# 账务中心规则


> 最后更新: 2026-05-12

## 当前证据边界

- 本文基于当前 Verify main 分支源码静态核对。
- 本轮已确认 `app/routers/accounting.py` 是当前账务中心正式路由。
- 本轮已确认 `tests/test_accounting.py` 存在并覆盖账务核心链路。
- 本轮未运行 pytest。
- 本轮未连接数据库确认旧物理表状态。
- 因此本文不得写"测试已通过""生产可用""旧物理表已删除"。

## 当前结论

✅ 新账务中心源码主线已建立。旧 ORM 已退出当前主线，新账务中心模型为当前代码口径。
⚠️ 旧物理表是否已在所有环境中删除，本轮未连接数据库确认，不能写"旧物理表已删除"。

## 当前主线（10 个账务 / 授权冻结相关模型）

- AccountingWallet：代理钱包余额快照（含 computed 列 available_credit/available_total）。
- AccountingDocument：业务单据（recharge/credit/freeze/unfreeze/authorization_charge/delete_refund/manual_adjust/reconciliation_adjust/reversal）。
- AccountingLedgerEntry：不可变账本明细（direction/in/out + entry_type + balance_type + idempotency_key 防重复）。
- AuthorizationChargeSnapshot：授权扣点快照（含返点追踪字段）。
- AccountingReconciliationRun / Item：对账运行和差异项。
- AccountingAdjustmentRequest：调账申请（含审批流）。
- AccountingRiskEvent：风险事件（含 evidence JSONB）。
- AgentMonthlyBill：代理月账单。
- AuthorizationFreezeRecord：授权停用后的剩余权益冻结记录（authorization_freeze_record；迁移 0023；路由 /admin/api/accounting/freezes）。

## 旧兼容层 — 旧 ORM 已删除，旧物理表状态待环境确认

> 说明：旧点数系统兼容层说明文档已删除。
> 当前账务主线以本文为准；旧字段、旧接口、旧 ORM 清理状态以 `[[01-网络验证系统/旧字段旧接口清理清单]]` 为准。

| 旧对象 | 状态 |
|--------|------|
| AgentBalance ORM | ❌ 已删除 |
| BalanceTransaction ORM | ❌ 已删除 |
| AuthorizationCharge ORM | ❌ 已删除 |
| balance_service.py | ❌ 已删除 |
| balance_admin.py | 当前 main.py 未注册旧 balance_admin 路由；旧文件物理存在状态本轮不作为本文事实判断 |
| balance_agent.py | 当前 main.py 未注册旧 balance_agent 路由；旧文件物理存在状态本轮不作为本文事实判断 |
| 前端 balance.js | ❌ 已迁移到 accountingApi |

## 已修复的风险

1. ✅ project_access_service 已读 AccountingWallet（`_get_agent_available_points`）。
2. ✅ user_service 扣点/返点直调 accounting_service（`consume_agent_authorization_points` / `refund_user_authorization_points_on_delete`）。
3. ✅ 前端 balance API 已全切 accountingApi（`/admin/api/accounting/*`）。
4. ✅ 幂等键已修复（`authorization:{id}:{snapshot.id}:charged`）。
5. ✅ 行锁全链路覆盖（`get_or_create_wallet(..., for_update=True)`）。
6. ✅ 对账基线初始化幂等（`init_baseline` 有 idempotency_key 防重复）。


## 对账服务 (app/services/accounting_reconciliation_service.py)

### 对账公式
- charged_balance = SUM(ledger charged 方向) 
- credit_balance  = SUM(ledger credit 方向, 不含 freeze/unfreeze)
- frozen_credit   = SUM(freeze) - SUM(unfreeze)

### 初始化基线 (init_baseline)
- 为每个有钱包余额的代理创建 adjust 类型基线账本记录
- 带 idempotency_key 防重复写入

### 运行对账 (run_reconciliation)
- 逐代理比对 wallet 快照 vs ledger 累计值
- 差异项记录 abnormal_count
- 单个代理失败不影响其他代理继续对账

### 查询
- GET /admin/api/accounting/reconciliation/runs — 对账批次列表
- GET /admin/api/accounting/reconciliation/runs/{id} — 批次明细
