## 阶段 A1：代理管理专项修复核查结果

核查来源已切换为 GitHub 最新代码：`nala74380/HiveGreatSage-Verify`，检索到的当前文件指向提交 `6526fff8eeca94de2465fd57199d362edcdc277c`。你上传的清单说明第 6 轮声称已修复 3 类问题：`agents.py` 创建顺序、`test_agents_recursive.py` 字段与 refresh 契约、`agent_scope_management.py` 父级授权过期检查。

## A1 核查结论

```text
A1 总结：未完全闭合，不能进入 A2。
```

### 1. `agents.py` 代理创建顺序修复：未修复 / 修复未进入当前 main

清单声称 `create_agent_in_scope_endpoint()` 已把前置校验移到创建前。

但 GitHub 当前 `agents.py` 仍然是旧顺序：

```text
先创建 child
db.add(child)
await db.flush()
await db.refresh(child)
然后才读取 current_profile
然后才判断 can_create_sub_agents / max_sub_agents
```

这意味着我之前指出的 bug 仍存在：`existing_subs` 会把刚 flush 的 child 也算进去，`max_sub_agents = 1` 时仍可能“第一个下级也创建失败”。代码片段显示 `child = Agent(...)`、`db.add(child)`、`await db.flush()` 在业务画像和 `max_sub_agents` 检查之前。

**判定：C. 未修复 / 或修复未同步到 GitHub main。**

DeepSeek 需要继续处理这一项。

---

### 2. `test_agents_recursive.py` 的 `level → hierarchy_depth`：未修复

清单声称 `test_agents_recursive.py` 已经完成 `level → hierarchy_depth`。

但 GitHub 当前测试仍断言：

```text
data["root"]["level"] == 1
root_node["level"] == 1
child1_node["level"] == 2
gc["level"] == 3
```

检索结果明确显示这些旧字段仍在测试里。

**判定：C. 未修复 / 或修复未同步到 GitHub main。**

DeepSeek 需要继续把测试里的 `level` 改为 `hierarchy_depth`，并同步测试说明文案。

---

### 3. `test_agents_recursive.py` 的 refresh 请求契约：已修复

这一项当前看起来已经修复。
`test_revoke_all_then_refresh_fails` 中的 `/api/auth/refresh` 请求体已经包含：

```json
{
  "refresh_token": refresh_token,
  "device_fingerprint": "test_agent_device_001",
  "client_type": "android"
}
```

检索结果显示 refresh 请求已补齐 `device_fingerprint` 和 `client_type`。

**判定：A. 已确认修复完整。**

---

### 4. 父级项目授权过期检查：未完全修复

清单声称 `_ensure_parent_project_scope()` 已增加父级授权过期检查。

但 GitHub 当前 `agent_scope_management.py` 中 `_ensure_parent_project_scope()` 仍只检查：

```text
parent_auth 是否存在
父级有到期时间时，下级不能永久
下级到期时间不能晚于父级到期时间
```

我没有看到：

```python
if parent_until is not None and parent_until <= now:
    raise HTTPException(...)
```

也就是说，如果父级授权已经过期但状态仍是 `active`，当前函数仍没有明确拒绝。

**判定：B/C. 部分修复未完成。**

如果 DeepSeek 已经修过，可能没有推到 `main`；如果推了，那当前 main 不是修复后的版本。

---

## A1 当前状态表

| 修复项                                                    | 当前核查结果                                         | 是否闭合  |
| ------------------------------------------------------ | ---------------------------------------------- | ----- |
| `create_agent_in_scope_endpoint()` 前置校验移到创建前           | 当前 main 仍是旧顺序                                  | ❌ 未闭合 |
| `max_sub_agents` 计数 bug                                | 当前 main 仍会把新 child 算入 existing_subs            | ❌ 未闭合 |
| `test_agents_recursive.py` 中 `level → hierarchy_depth` | 当前 main 仍有 `level` 断言                          | ❌ 未闭合 |
| refresh 请求补齐字段                                         | 当前 main 已补齐 `device_fingerprint / client_type` | ✅ 闭合  |
| 父级项目授权过期检查                                             | 当前 main 未见 `parent_until <= now` 拦截            | ❌ 未闭合 |

## 给 DeepSeek 的返工清单

### 必须返工 1：`app/routers/agents.py`

目标：`create_agent_in_scope_endpoint()` 必须改成先校验、后创建。

正确顺序应为：

```text
1. username 唯一性检查
2. 读取 current_profile
3. 检查 can_create_sub_agents
4. 创建前统计直属下级数量
5. 检查 max_sub_agents
6. 计算 child_tier_level
7. 检查 child_tier_level >= 1
8. 然后才 child = Agent(...)
9. db.add(child)
10. flush / refresh
11. 创建 AgentBusinessProfile
12. 写 audit_log
```

### 必须返工 2：`tests/test_agents_recursive.py`

把仍然存在的：

```python
["level"]
```

全部改为：

```python
["hierarchy_depth"]
```

并把测试描述里的“level 字段”改成“hierarchy_depth 字段”。

### 必须返工 3：`app/routers/agent_scope_management.py`

在 `_ensure_parent_project_scope()` 中补明确过期检查：

```python
now = _now()
if parent_until is not None and parent_until <= now:
    raise HTTPException(
        status_code=status.HTTP_403_FORBIDDEN,
        detail="父级项目授权已过期，不能给下级开通或更新该项目授权",
    )
```

## 是否进入 A2？

不建议进入 A2。
A1 只有 1 项完全闭合，3 项仍未闭合。先让 DeepSeek 按上面三项返工，并确认已经推送到 GitHub `main` 或提供明确分支/提交号后，我再进行 A1 复核。

---

## Claude 本地核查补充 (2026-05-11)

对 ChatGPT A1 核查的本地文件复验：

### 核查环境差异

- **ChatGPT 核查源**: GitHub `nala74380/HiveGreatSage-Verify` main 分支 (commit `6526fff`)
- **Claude 核查源**: 本地 `E:\HiveGreatSage\...\HiveGreatSage-Verify` (非 git 仓库)

### 本地文件逐项复验

| # | 修复项 | 本地文件 | 本地状态 |
|---|--------|---------|---------|
| 1 | agents.py 创建顺序 | `agents.py:898` — "前置校验：业务画像与权限" | ✅ 已修复 |
| 2 | test level→hierarchy_depth | `test_agents_recursive.py:137` — `hierarchy_depth` | ✅ 已修复 |
| 3 | refresh 契约 | `test_agents_recursive.py:401-404` | ✅ 已修复 (GitHub 也确认) |
| 4 | 父级授权过期检查 | `agent_scope_management.py:395-399` | ✅ 已修复 |

### 根因

本地修复文件与 GitHub 远程 `nala74380/HiveGreatSage-Verify` main 分支不同步。
本地项目目录非 git 仓库，修复仅在本地文件系统中生效。
ChatGPT 核查的是 GitHub 远程，因此看到的是旧代码。

### 建议

需将本地修复文件同步到 GitHub 仓库 (`nala74380/HiveGreatSage-Verify`)，ChatGPT 的后续 A1 复核才能通过。
