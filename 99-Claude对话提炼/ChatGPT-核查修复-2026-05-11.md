## A1 本地上传文件复核结论

**已确认：这次你上传的 3 个本地修复文件中，A1 的 3 项核心修复已经完成。**

但注意边界：这是对 `/mnt/data` 上传文件的静态核查结论，**不是 GitHub `main` 结论**。只有同步到 GitHub 后，后续我才能把 GitHub 核查状态也改为通过。

---

## 1. `agents.py`：代理创建顺序与 `max_sub_agents` 修复

**结论：已修复。**

上传版 `agents.py` 中，`create_agent_in_scope_endpoint()` 已经改成先做前置校验，再创建 `Agent`：

```text
1. 用户名唯一性检查
2. 读取 current_profile
3. 检查 can_create_sub_agents
4. 创建前统计 existing_subs
5. 检查 max_sub_agents
6. 计算 child_tier_level
7. 再创建 child = Agent(...)
8. 再 db.add / flush / refresh
9. 再创建 AgentBusinessProfile
```

这已经修复我之前指出的核心问题：**新 child 不会再被提前 flush 后计入 existing_subs**。因此 `max_sub_agents = 1` 时，理论上可以创建第一个下级，第二个才会被拒绝。

**状态：A. 已确认修复完整。**

---

## 2. `test_agents_recursive.py`：`level → hierarchy_depth`

**结论：核心断言已修复。**

上传版 `test_agents_recursive.py` 中，旧的：

```python
["level"]
```

已经不再作为断言字段出现，核心断言已改为：

```python
data["root"]["hierarchy_depth"] == 1
root_node["hierarchy_depth"] == 1
child1_node["hierarchy_depth"] == 2
gc["hierarchy_depth"] == 3
```

这和当前“不兼容旧字段 `level / hierarchy_level`”的契约一致。

**状态：A. 已确认修复完整。**

### 顺手发现的小文案问题

测试函数文案仍写：

```text
各层 level 字段应正确（L1→L2→L3）
```

建议改成：

```text
各层 hierarchy_depth 字段应正确（L1→L2→L3）
```

这不是功能阻断，但建议同步，避免以后误导。

---

## 3. `test_agents_recursive.py`：refresh 请求体契约

**结论：已修复。**

上传版测试中，`/api/auth/refresh` 请求体已经补齐：

```json
{
  "refresh_token": "...",
  "device_fingerprint": "test_agent_device_001",
  "client_type": "android"
}
```

这符合当前 `RefreshRequest` 的契约。

**状态：A. 已确认修复完整。**

### 顺手发现的小文案问题

`test_revoke_all_me_still_works_briefly` 的 docstring 还写“`/me` 应仍然返回 200”，但下面断言实际是 `401`。这和当前 token blacklist / token_version 方向不一致。

建议把说明改成：

```text
revoke-all 后当前 AT 已写入黑名单，再次访问 /me 应返回 401。
```

这是文案漂移，不影响本次 A1 核心修复判定。

---

## 4. `agent_scope_management.py`：父级项目授权过期检查

**结论：已修复。**

上传版 `_ensure_parent_project_scope()` 已经加入明确检查：

```python
if parent_until is not None and parent_until <= datetime.now(timezone.utc):
    raise HTTPException(
        status_code=status.HTTP_403_FORBIDDEN,
        detail="当前代理的项目授权已过期，不能给下级开通",
    )
```

这已经覆盖我之前指出的风险：父级授权虽然 `status == active`，但 `valid_until` 已经过期时，不能继续给下级开通项目授权。

**状态：A. 已确认修复完整。**

---

## A1 最终状态

| 核查项                            | 本地上传文件状态 |
| ------------------------------ | -------- |
| 代理创建前置校验顺序                     | ✅ 已修复    |
| `max_sub_agents` 计数 bug        | ✅ 已修复    |
| `level → hierarchy_depth` 测试断言 | ✅ 已修复    |
| refresh 请求体补齐                  | ✅ 已修复    |
| 父级项目授权过期检查                     | ✅ 已修复    |

## 仍需同步处理

```text
1. 把这 3 个文件同步到 GitHub main。
2. 顺手修正 test_agents_recursive.py 中两个旧文案：
   - “level 字段” → “hierarchy_depth 字段”
   - revoke-all 后 /me “应返回 200” → “应返回 401”
3. 同步后我再按 GitHub main 复核一次。
```

**当前判定：A1 本地修复通过；GitHub 状态待同步后复核。**
